
// Serviço de Scraper removido.
export interface SocialStats {
  followers: string | null;
}

export const scrapeInstagram = async (username: string): Promise<string | null> => null;
export const scrapeTikTok = async (username: string): Promise<string | null> => null;
export const scrapeYoutube = async (channelHandle: string): Promise<string | null> => null;
